{
  "name": "@agentcomms/client",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -b && vite build",
    "dev": "vite --host 127.0.0.1",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentcomms/protocol": "workspace:*",
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "vite": "^6.0.7"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
